{
    "vsicons.presets.angular": false,
     "files.exclude": {
        "**/.git": true,
        "**/.DS_Store": true,
        "**/*.js.map": {"when": "$(basename)"},
        "**/*.js": {"when": "$(basename).ts"}
    }
}